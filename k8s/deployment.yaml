apiVersion: apps/v1
kind: Deployment
metadata:
  name: 
spec:
  replicas: 2
  selector:
    matchLabels:
      app: 
  template:
    metadata:
      labels:
        app: 
    spec:
      containers:
      - name: exam2026
        image: gcr.io/icai2026/exam2026:latest
        imagePullPolicy: 
        ports:
        - containerPort: 5001
        env:
          - name: GCP_BUCKET_NAME
            value: "REPLACE_BUCKET_NAME"
        livenessProbe:
          periodSeconds: 
          httpGet:
            path: /probe
            port: 


